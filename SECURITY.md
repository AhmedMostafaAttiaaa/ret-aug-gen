# Security Policy

If you discover a security vulnerability in this project, please open a private report via GitHub's Security Advisories tab rather than a public issue.

Never commit API keys or secrets — this project reads them from a local `.env` file, which is gitignored.
